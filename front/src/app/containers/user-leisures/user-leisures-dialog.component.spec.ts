import { UserLeisuresDialogComponent } from './user-leisures-dialog.component'

describe('UserLeisuresComponent', () => {
  it('should mount', () => {
    // cy.mount(UserLeisuresDialogComponent)
  })
})
